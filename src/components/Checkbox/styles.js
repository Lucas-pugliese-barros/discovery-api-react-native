import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  checkBox: {
    flexDirection: 'row',
    alignItems: 'center',
    borderRadius: 6,
    width: 20,
    height: 20,
  },
});

export default styles;
